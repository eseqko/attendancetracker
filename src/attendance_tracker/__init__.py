"""Core logic for the attendance tracker: parsing, detection, matching, metrics.

Everything in this package is plain pandas/numpy — no Streamlit imports — so it
can be unit-tested and reused outside the UI.
"""

__version__ = "0.1.0"
